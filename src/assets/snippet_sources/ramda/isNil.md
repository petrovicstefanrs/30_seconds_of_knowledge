### isNil

> `* → Boolean`

Checks if the input value is `null` or `undefined`.

`Example`

```js
R.isNil(null); //=> true
R.isNil(undefined); //=> true
R.isNil(0); //=> false
R.isNil([]); //=> false
```
